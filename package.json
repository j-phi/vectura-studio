{
  "name": "vectura-studio",
  "version": "0.6.79",
  "private": true,
  "description": "Vectura Studio - physics-inspired vector generator",
  "scripts": {
    "version:sync": "node scripts/sync-version.js",
    "patch:test-runtime": "node scripts/patch-vitest-unicode.js",
    "profiles:bundle": "node scripts/build-petal-profile-library.js",
    "postinstall": "npm run patch:test-runtime",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "npm run patch:test-runtime && vitest run tests/unit",
    "test:integration": "npm run patch:test-runtime && vitest run tests/integration",
    "test:e2e": "npm run patch:test-runtime && playwright test tests/e2e/smoke.spec.js",
    "test:visual": "npm run patch:test-runtime && vitest run tests/visual",
    "test:visual:screenshots": "npm run patch:test-runtime && ENABLE_SCREENSHOT_VISUALS=1 playwright test tests/e2e/visual.spec.js",
    "test:perf": "npm run patch:test-runtime && vitest run tests/perf",
    "test:ci": "npm run test:unit && npm run test:integration && npm run test:e2e",
    "test:update": "npm run patch:test-runtime && VECTURA_UPDATE_BASELINES=1 npm run test:visual"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "autoprefixer": "^10.4.27",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^24.1.1",
    "postcss": "^8.5.8",
    "prettier": "^3.2.5",
    "tailwindcss": "^4.2.1",
    "vite": "^5.4.8",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
