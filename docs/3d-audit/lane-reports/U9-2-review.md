STATUS: ACCEPT

# U9-2 — adversarial review

Worktree reviewed (read-only): `.claude/worktrees/handoff-c2`, branch `3d-scene/handoff-c2`, pinned
range **fc8b0fba..ed778940** (single commit `ed778940`). Verified in two scratch exports
(`/private/tmp/claude-501/scratch-U9-2-fc8b0fba`, `.../scratch-U9-2-ed778940`, `node_modules`
symlinked), never by editing/stashing the worktree. Scratch dirs and a scratch dev-server (port 8471)
removed after verification.

## (1) Guard shape, floor+band, tied to a measured count, trips under the named mutation

Confirmed against the diff (`tests/unit/scene3d-shadow-tone-law.test.js`, new test `'U9-2 — folded-id
shadow recipe delta on shadowPathCount has a floor + fingerprint band...'`, inserted immediately after
`HEADLINE (U9)`, nothing else in the file touched):

- **New bar, not a re-pin.** `ladderCount === 116` and `fineCount === 120` did not exist before this
  commit (confirmed against fc8b0fba's copy of the file — no such assertions).
- **Floor below the drift envelope + ±10% band, both pinned to measured absolute counts:**
  `deltaAbs > 1` (floor) and `deltaAbs` in `[3.6, 4.4]` (±10% band around a measured baseline
  `deltaAbs = 4`, i.e. `116`→`120` on the file's own `buildShadows` fixture). Re-ran
  `tests/unit/scene3d-shadow-tone-law.test.js` on the unmutated ed778940 export: **23/23 passing**
  (matches the implementer's claim), including this new test — `ladderCount=116`, `fineCount=120`
  reproduce exactly.
- **Trips under the named mutation.** Reproduced the implementer's exact one-line inverse of the U9 fix
  in `src/core/scene3d/params.js` line 1050
  (`shadowToneLaw: clampShadowToneLaw(src.shadowToneLaw)` → `clampStyleParam('toneLaw',
  src.shadowToneLaw)`) in the scratch export and re-ran the same test file: **3 failing / 20 passing**
  (of 23) — the pre-existing `HEADLINE (U9)` geomSignature test, the pre-existing params-level `U9 —`
  folded-id test, and the new U9-2 guard, which fails at its very first assertion
  (`expected 116 to be 120`, i.e. `fineCount` collapses to `ladderCount`, `deltaAbs` would be 0) —
  exactly the failure mode and exactly the numbers the implementer reported. Restored the file and
  reconfirmed 23/23 on the unmutated tree before moving on (an earlier verification pass of mine left
  the mutation in place by mistake; caught and corrected before drawing any conclusion — noted here for
  transparency, not a finding against the unit).

Verdict: this condition is met precisely as claimed.

## (2) Script move: rename not copy, paths fixed, byte-identical re-run

- `git diff -M --stat fc8b0fba..ed778940` → `scripts/{ => audit}/u9-shadow-resolve-evidence.js | 10
  ++++--` — a genuine rename (95% similarity), not an add+delete or copy.
- Confirmed via `git archive` on both endpoints: the file exists only at
  `scripts/u9-shadow-resolve-evidence.js` in fc8b0fba and only at
  `scripts/audit/u9-shadow-resolve-evidence.js` in ed778940.
- The only content change is the `outDir` default gaining one extra `'..'`
  (`path.resolve(__dirname, '..', 'docs', ...)` → `path.resolve(__dirname, '..', '..', 'docs', ...)`),
  correct for the new nesting depth, plus a comment block. Nothing else in the script references its
  own path.
- **Re-run proof (own reproduction, not just re-reading the claim):** started a scratch dev server on
  the ed778940 export (port 8471), ran `node scripts/audit/u9-shadow-resolve-evidence.js
  http://localhost:8471 <scratch-out>`. First attempt gave `147/147/190` (my own mutation from step
  (1) was still in the tree at that point — my error, not the implementer's); after restoring the file,
  re-ran and got:
  `SUMMARY {"ver":"1.3.99","beforeShadowPathCount":147,"afterShadowPathCount":151,
  "controlShadowPathCount":190,"afterDiffersFromBefore":true,"controlDiffersFromBefore":true}` —
  matches the committed `docs/3d-audit/fill-audit/after/U9/stats.json` numbers exactly (147/151/190,
  both `*DiffersFromBefore: true`). `diff`'d the full `stats.json` produced by my run against the
  committed one: identical in every field (`shadowPathCount`, `geomSig`, `shadowBB`) except the three
  `layerId` UUIDs, which are freshly randomly generated by `engine.addLayer()` on every run by design —
  not a byte-identity defect. This is the expected shape of "byte-identical" for a script that
  allocates a fresh layer id each invocation; the implementer's report correctly scoped the claim to
  the `SUMMARY` numbers.

Verdict: met, with the caveat (not a finding) that "byte-identical" applies to the SUMMARY/measurement
fields, not the full stats.json (layerId is non-deterministic by the script's own design, present
before this unit too).

## (3) No `src/` change in range; `## Bars changed` honest

`git diff -M --stat fc8b0fba..ed778940` touches exactly two files:
`scripts/{ => audit}/u9-shadow-resolve-evidence.js` and `tests/unit/scene3d-shadow-tone-law.test.js`.
No `src/` file appears in the range. The impl report's `## Bars changed` section correctly declares
this a **NEW bar** (`shadowPathCount` had no bar of its own per the U9 review) and lists exactly the
four new assertions (`ladderCount===116`, `fineCount===120`, floor, band) — confirmed against the diff;
no existing assertion elsewhere in the touched file was widened, narrowed, or re-pinned.

## (4) Guards re-run by reviewer

All four re-run independently in the scratch export, foreground, one file at a time:

- `tests/unit/scene3d-shadow-tone-law.test.js` — **23/23 passing** (unmutated).
- `tests/unit/scene3d-shadow-tone-law-uniqueness.test.js` — **3/3 passing**, unmodified.
- `tests/unit/scene3d-tone-law-collapse.test.js` — **54/54 passing** — confirmed. This run also hit the
  harness's 120s foreground-timeout auto-background (twice, cascading, since my own wait-loop was also
  auto-backgrounded) under ~471s real duration; the same benign
  `[vitest-worker]: Timeout calling "onTaskUpdate"` teardown-noise line appeared after all 54 tests had
  already reported passing, exit code 0 — consistent with the implementer's report and with
  AGENT-PROTOCOL's "timeouts under load are not regressions" note. Never used `run_in_background` or
  Monitor by choice; the harness's own timeout auto-backgrounded the command, and I waited for
  completion rather than proceeding without the result.

## Hygiene note (non-blocking)

During my own verification I twice left the params.js mutation in the scratch tree instead of
restoring it, which produced a false-looking script result on the first pass. Caught immediately by
re-checking the file and re-running; does not reflect on the implementer's work, which reproduces
cleanly. Logged here only for transparency per the reviewer protocol's numbers-not-vibes standard.

## Verdict

**ACCEPT.** The guard is a genuinely new bar (not a re-pin), pinned to measured absolute counts
(116/120, delta 4), sits a floor below the measured 2–4-path drift envelope, adds a ±10% fingerprint
band on top, and trips at its very first assertion under the exact mutation the implementer names
(reverting `shadowToneLaw` normalization to the plain `clampStyleParam('toneLaw', …)` path) while
staying green on the unmutated tree. The script relocation is a true rename with the one necessary
relative-path fix, and re-running it from its new location reproduces the committed U9 evidence numbers
exactly. No `src/` change in range; the `## Bars changed` declaration is accurate. All four named
guard files re-run clean by the reviewer independently.
