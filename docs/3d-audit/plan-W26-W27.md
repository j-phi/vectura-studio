# Plan — W-26 (irregular gaps) / W-27 (polygonal contourSlice) — from opus diagnosis 2026-09-05

## W-26 root cause
Ladder selects a SUBSET of a fixed master grid (surface-fill.js:5084-5118: tonePitch = hatchSpacing(d)/TONE_SUBDIV, masterPitch = min(tonePitch, o6Pitch), N = round(calib/masterPitch)). At app default (pen 0.3, d=50) tonePitch ≈ 1.5 mm ≈ o6Pitch 1.51 mm, so the grid the ladder subsets is within 1% of the finished spacing: every keep/drop is a whole-pitch step. Selection: ladderStep :1272-1275 (Bresenham phase accumulator), ladderKeeps :6056-6079, one verdict per ruling :7986, coverage from covAtSample :7718-7738 → coverageForSample :1582-1592 (a STEP function of the tone band; defaults.js:2116 tone.ladder=[0.2,0.5,0.85]).
At constant coverage c the ladder emits the Sturmian word of density c → gaps floor(1/c) and ceil(1/c) pitches → 2× white band in constant-tone regions. tests/unit/scene3d-fill-even-spacing.test.js pins exactly this (even INDEX subset) — green suite, wrong render.
Measured: capsule contour ladder med: 23.0,23.5,46.5,23.0,23.5,46.5 px (c=0.75); capsule spiral ladder: 8.5×3,17.0 (c=0.80); cone contour amplitudeOnly: 22.x×5, 56.5, 34, 45.5 (~0.8). Hand-run ladderStep from LADDER_PHASE0=0.5 reproduces exactly. Dark rung 0.85 → 1,1,2,1,1,1,1,1,1,2.
Gradient? Provably not: charts.js:46-53 cone normal = f(λ) only, independent of u; contour rules constant-u rings (surface-fill.js:10272-10273 → emitFamily('a') :8967-8975) → every ring identical coverage. Same for cylinder and capsule barrel (charts.js:95-101). 100% quantisation, 0% tone. New finding F-23.
Aggravators: covAtSample plot-floor clamp :7766-7784 drops rulings; continuousPitch :4642 only changes coverage number, placement still subsets grid :10250-10275; fineLadder :1679-1707 adds tone rungs not placement; W-01 (16c197d7) makes masterPitch coarser below d=50 → wider doubled gaps. Refining the grid can't fix it (floorPen = PLOT_FLOOR_PEN·pen = 0.66 mm; grid ~1.5 mm → at most 2.3× refinement → 1.4× ratio). Only continuous placement removes it.

## W-27 root cause
scene3d.js:135-168 buildSliceSegments emits triangle-cut crossing points verbatim; :3395-3402 links per plane and projects. Ring vertex count = mesh edges crossed (~7 near sphere front pole at detail 16, sliceCount 26). No resample/subdivide/smooth. Rounding is not "off": Engine._applySceneCurveFinish (engine.js:2865-3019) fits sceneFill paths when style has fillCurves and primitive ∈ CURVED_FILL_PRIMITIVES, BUT (1) geometry-utils.js:2441 acceptable() requires curved*2 > spans; a 7-gon reads as all corners → fitPathAnchors returns {straight:true} :2477-2479; (2) fillCurves:true comes from LINE_FINISH_CREATE_DEFAULTS (scene3d/params.js:187) via seedLineFinishDefaults (engine.js:517-533) on the CREATION path only — the audit harness builds q.objects directly (scene3d-capture.js:210-220) so all contourSlice shots had finish off; (3) CURVED_FILL_PRIMITIVES gate blocks solid/importedMesh even though a plane cut through a baked sphere is round.

## Rules
R1 (W-26): for ladder/fineLadder/phaseFineLadder (and any selecting law) on hatch/crosshatch/contour/spiral, drawn spacing must be monotone in the intensity field.
 R1a uniform field (adjacent rulings' span-mean I differ <1e-3): drawn gaps max/min ≤ 1.15 (today 2.02 on cone+contour, cylinder+contour).
 R1b no inversion: no skipped ruling between two drawn rulings closer to the light than itself.
 R1c tone survives: whole-form max/min drawn gap ≥ 2 on a sphere; ink within ±15%.
R2 (W-27): every contourSlice ring on a smooth surface: max exterior turning angle ≤ 12° (today 45-60° pole-adjacent) and fitPathAnchors(ring).straight !== true; plane-count purity tests (scene3d-contour-slice.test.js:204-306) unchanged.

## W-26 brief  [L]  lane: fill-audit-a AFTER W-05/06/07 and W-01 M1 land (surface-fill.js serialized)
Touch: surface-fill.js ONLY algoCoverage (:4637 one clause), cfWantedPitch / walk pitch source used by emitContFamily (:9092-9330), mapper dispatch :10197-10275, spiral turn-drop :10339-10361; scene3d-tone-laws.js + context-bar.js only if a law id is added; new test file.
NOT: surface-fill.js:5084-5157 (W-01 master grid, lastMasterGridStats), coverageForSample :1582, zoneCoverage :4797, ladderStep :1272, ladderKeep :1302, spanDrops :7151, emitMarks/MK.* sinks, surface-fill-mono.js, mappers.js, hlr.js, ray-torus.js.
Change (opus staged as new law `ladderEven`; ORCHESTRATOR DECISION: the user said the gaps "must not exist", so apply continuous placement to the ladder family itself — ladder, fineLadder, phaseFineLadder — not a new roster entry; fingerprints that change must be re-pinned WITH proof in the commit body: counts before/after + the R1a/R1c numbers):
 1. algoCoverage returns 1 for these laws when placement is continuous (same shape as isContField() :4689 — "the spacing IS the tone").
 2. wantedPitch(I) = clamp(masterPitch / clamp(ladderCov(I), covMin, 1), floorPitch, litMaxPitchPen()·penWidth), ladderCov = the coverage the ladder computes TODAY unchanged (zone ? zoneCoverage : coverageForSample) — tone identical by construction, only placement moves.
 3. Route hatch/crosshatch/contour through emitContFamily (:9092) as contMapper does :10219-10236, incl. crosshatch second family.
 4. Spiral: remove whole-turn drop :10359; warp sweepAt (:10307-10311) so axial advance per turn = wantedPitch(I)/mmPerSweepUnit; recompute turns so integrated sweep reaches 1.
RGR red test FIRST: tests/unit/scene3d-ladder-uniform-field-spacing.test.js (rig from tests/fixtures/scene3d-shadow-anatomy): lemma — cone+contour & cylinder+contour span-mean I max-min <1e-3 (cite charts.js:46-53, :89-101); R1a via keptByFamily (from scene3d-fill-even-spacing.test.js) projected perpendicular gaps max/min ≤1.15 (red today 2.02); capsule barrel variant (rulings within 1e-3 I) red 2.02; spiral on cone turn advance ratio ≤1.15 (red 2.00); R1c guard sphere ≥2; vacuous guards paths>0, weightScale===1, before/after not byte-identical.
Guards to keep green (or re-pin with proof): scene3d-hlr-spatial-index-identity, scene3d-tone-algo-default, scene3d-curved-density-floor, scene3d-curved-density-sparse-end, scene3d-form-ladder, scene3d-fill-even-spacing (this one pins the OLD index-even behaviour — it must be REPLACED by the drawn-gap rule with proof, not deleted), scene3d-fill-span-verdict, scene3d-fill-ruling-continuity, scene3d-fill-seam-continuity, scene3d-plot-floor-obj, scene3d-plot-safety, scene3d-subwindow-density, scene3d-appdefault-lit-floor, scene3d-ribbon-f7-self-occlusion.
Re-shoot: shots/A/capsule__{contour,hatch,crosshatch,spiral}__ladder__{low,med}__a, shots/B/cone__contour__{ampSpacing,amplitudeOnly}__med__a, new controls cone__contour__ladder__med__a, cylinder__contour__ladder__med__a.
Stop if: walk dfMax ceiling (:9245) binds leaving bare patches (ellipsoid-contour failure :9236-9244); any ruling ends in open front surface (scene3d-fill-boundary-ends); ink >+15%; wallRings===0/bare centrelines; uniform-field lemma fails. Never widen 1.15.

## W-27 brief  [S/M]  lane: fill-audit-d (disjoint, parallel)
Touch: src/core/algorithms/scene3d.js (buildSliceSegments :110-168, contourSlice pass :3359-3440); optionally a smoothSurface flag from mesh.js/scene.js onto the record; tests/unit/scene3d-contour-slice.test.js; scripts/audit/scene3d-capture.js (MAIN) only if the harness-seed gap is confirmed (seed LINE_FINISH_CREATE_DEFAULTS so shots match the app).
NOT: surface-fill*.js, mappers.js, geometry-utils.js fitter (do not loosen acceptable() :2441).
Step 0: app on port 8481, add sphere from shelf (seedLineFinishDefaults fires), Type=Slices, screenshot; vs harness programmatic scene. Report both. If app render already round, scope = CURVED_FILL_PRIMITIVES gate for converted/imported meshes + fitter declining coarse rings.
Fix: refine each linked ring in WORLD space before projection (scene3d.js:3395-3402): two rounds of Chaikin or Catmull-Rom resample to ~1.5 mm chord, when the ring's source surface is smooth (derive from mesh record: chart-wrapped primitives + welded topoform bakes smooth; box/plane/faceted solid not) — NOT from CURVED_FILL_PRIMITIVES. On by default for smooth surfaces; hard corners on faceted cuts survive.
RGR: extend scene3d-contour-slice.test.js: sphere r20 detail16 sliceCount26 — every ring ≥4 pts max exterior angle ≤12° (red 45-60°); fitPathAnchors(ring).straight !== true (red true); accuracy: max radial deviation from analytic circle √(r²−z²) SMALLER than raw polyline's (Chaikin shrinks otherwise); box cut keeps corners ≥80°.
Guards: plane-count purity tests :204-306 (sliceCount → plane count pure; orbit/draft/occluders/detail must not change it); scene3d-curves, scene3d-hlr, scene3d-hlr-spatial-index-identity.
Re-shoot: shots/B/sphere__contourSlice__etfKang__med__a, another contourSlice cell, shots/B/torus__contourSlice__none__med__a, plus a solid/converted case.
Stop if: refinement moves a point off the true surface >0.5 pen; HLR clip cost passes SLICE_CLIP_WORK (scene3d.js:180) and planes emit raw; W-02's gate makes contourSlice unreachable in picker (coordinate).

## Reviewer checklist
1 uniform-field lemma asserted not assumed; 2 R1c + ink ±15% (no flattening); 3 vacuous pass guards + screenshot; 4 no weakened metric / fingerprint w/o proof; 5 W-26 must not touch :5084-5157; 6 tone transfer unchanged (same coverageForSample/zoneCoverage); 7 spiral: turn dropped vs pitch warped — 8.5/17.0 alternation gone; 8 coverage oracle check (handoff finding 2); 9 W-27 fitter gate untouched; 10 ring more accurate not just smoother; 11 evidence one pipeline one zoom 3.6 object-only; 12 settled items untouched (tone 39.4%, coverage contract, spiral default).

## W-26 addendum (planner correction, 2026-09-05 evening)
- Instrument `ladderKeeps` from a vitest file (wrap `SF.buildObject` as `scene3d-fill-even-spacing.test.js` does in its `emittedRuns` helper), NOT from a bare `node -e` (the runtime loader needs the harness).
- Evidence gap: the coverages above (0.75 capsule barrel, 0.80 spiral) were INFERRED by matching pixel gap words to `ladderStep` run by hand from LADDER_PHASE0=0.5; shipped rungs are [0.2,0.5,0.85]. W-26's FIRST task: dump actual per-ruling `cov` and confirm (if the barrel reads 0.85 the mechanism is unchanged, the numbers differ).
- `tests/unit/scene3d-fill-even-spacing.test.js` tolerates this defect by construction: it asserts |gap[i]−gap[i−1]| > 1 on KEPT-INDEX gaps (a 1 next to a 2 is allowed) and runs a light ladder [0.08,0.18,0.3,0.45] at Density 70, not the shipped default; its cylinder case passes even though the cylinder field is uniform (charts.js:89-95). W-26's red test supersedes that blind spot: pin the DRAWN projected gap ratio (max/min ≤ 1.15) on a proven-uniform field at the SHIPPED ladder and Density 50.
- Reviewer checklist item 13: if the new test passes without `scene3d-fill-even-spacing` needing any edit, check it is measuring drawn spacing, not index gaps again.
