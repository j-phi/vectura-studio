# Pre-Release Hardening Log

This log tracks hardening ideas that should be completed before the final public release, but are intentionally deferred during beta to avoid slowing development.

- Purpose: track release hardening work, not beta delivery work.
- Enforcement: no CI gate and no release blocker behavior during beta.
- Ownership: human-maintained process log.

## Item Schema

Each entry must include:

- `id` (format: `PRH-001`, `PRH-002`, ...)
- `idea`
- `why_pre_release_only`
- `beta_risk_likelihood` (`low`, `medium`, `high`)
- `impact_if_missed_at_release`
- `proposed_change`
- `verification_before_final_release`
- `status` (`logged`, `selected`, `done`, `dropped`)
- `links` (PRs, issues, test files)

## Current Items

| id | idea | why_pre_release_only | beta_risk_likelihood | impact_if_missed_at_release | proposed_change | verification_before_final_release | status | links |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PRH-001 | Add E2E export->import bounds verification for hard-cropped margin exports. | Low probability during beta and adds test maintenance complexity now; primarily valuable as final hardening. | low | Margin overflow regressions could slip into public release and only appear in downstream export workflows. | Add a Playwright E2E that exports with `Crop Exports to Margin`, re-imports output, and asserts all drawable coordinates stay within configured margins. | E2E test passes in CI/local before final release and fails when margin overflow is reintroduced. | logged | `tests/integration/export-hard-crop.test.js` (current integration coverage baseline) |
| PRH-002 | Surface Pathfinder Options (FillBoolean precision, "Remove Redundant Points", "Divide and Outline Will Remove Unpainted Artwork") as a user-controllable dialog. | Pathfinder v1 bakes Illustrator-parity defaults (epsilon = FillBoolean internal, keep all fragments, no redundant-point pruning) which work for typical plotter line art; only edge-case workflows would notice. | low | Power users importing high-density SVG art may want tighter precision or to drop unpainted slivers; not having the dialog risks surprise output divergence vs Illustrator on those imports. | Add a small modal from a Pathfinder panel-header menu (mirror Illustrator's flyout) exposing precision (0.001–100 pt) and the two toggles; thread values through `Vectura.PathfinderOps.applyPathfinder` call sites. | Modal toggles round-trip into op output (smaller precision → fewer collapsed points; "drop unpainted" → empty cells removed); regression test against fixed FillBoolean precision. | logged | `docs/pathfinder-requirements.md` §8, `src/core/pathfinder-ops.js`, `src/ui/panels/pathfinder-panel.js` |
| PRH-003 | Preset Save Phase 2 — folder-on-disk persistence via the File System Access API (pick a folder, IndexedDB-stored handle, "Reconnect Thaw" re-permission flow, one-way write to `<folder>/<system>/<slug>.vectura`) with a Safari/Firefox export/import-bundle fallback, plus the contributor direct repo-folder write. | Phase 1 (save pip + modal + localStorage + dev-mode download) already delivers easy universal saving across all 18 algorithms; folder persistence is Chromium-only and carries the riskiest surface (per-session permission re-prompt, IndexedDB handle lifecycle), so it was deliberately phased after Phase 1 ships and proves demand. | low | Users who clear browser storage lose their localStorage-only presets; without folder backing or a periodic export nudge, that loss is silent. | Add a Settings "Preset Storage" tri-state (`Browser only` / `Save to a folder` / `Export bundle`); persist the `FileSystemDirectoryHandle` in IndexedDB; mirror every save to disk one-way (last-write-wins, no sync engine); add the launch-time Reconnect banner gated on a user gesture; dev-mode gains a direct repo-folder destination with a live `user-presets/<type>/<slug>.vectura` breadcrumb. | Chromium: folder connect persists across reload after one Reconnect click; saves land on disk; Safari/Firefox fall back to export/import bundle with no broken pill. | done (v1.1.64 — folder persistence + reconnect + export/import bundle shipped; contributor *direct* repo-folder write still deferred, the Phase-1 dev-mode download path covers authoring built-ins) | `src/ui/preset-folder-store.js`, `src/ui/preset-bundle.js`, `src/ui/modals/document-setup.js`, `src/ui/components/harmonograph-preset-gallery.js`, plan `for-our-presets-for-async-mist.md` |
| PRH-004 | Preset Save delight follow-ups — (a) per-preset "browser only, won't survive a cache clear" marker on localStorage-only presets, shown only if user testing reveals confusion; (b) Shadow History: an auto-snapshot ring buffer of recent unsaved looks surfaced as a "Recent (unsaved)" gallery group. | Both are net-additive polish beyond the core save gap; (a) risks badge clutter on a storage model that doesn't exist until PRH-003, and (b) introduces a second preset-adjacent surface + quota/dedupe complexity that would compete with the core flow. | low | Without (a) users may not realize localStorage presets are ephemeral; without (b) users who forget to save a look they liked can't recover it. | (a) one quiet icon + accessible text in the label area (not the thumbnail corner), folder-backed/built-in presets get nothing; (b) a capped ring buffer keyed per system with dedupe, rendered as a distinct gallery group with one-click promote-to-saved. | (a) marker appears only on localStorage-only presets and reads correctly with a screen reader; (b) editing then abandoning a look leaves a recoverable entry that promotes to a real user preset. | logged | `src/ui/components/harmonograph-preset-gallery.js`, plan `for-our-presets-for-async-mist.md` §"Top ideas / runner-ups" |
| PRH-005 | Apply a Morph group's own ancestor (non-morph) modifiers to its `morphedPaths`. | A Morph group nested *under* another modifier (e.g. Mirror) is an advanced composition not in the Morph MVP; the per-leaf modifier reduce skips groups and the morph post-pass writes only `group.morphedPaths`, which no outer reduce consumes — so morph rings render un-mirrored. Fixing it cleanly needs the morph post-pass to apply the group's ancestor non-morph modifiers (and reconcile with the mask-boundary split), which carries its own untested surface. | low | Users who nest a Morph group inside a Mirror (or other) modifier get silently un-transformed morph output; the inner direction (modifier *inside* morph) already works because the leaf bakes it. | In `_computeMorphGroups`, after computing `morphedPaths`, reduce the group's ancestor non-morph modifiers over it via `applyModifierToPaths`, respecting `_splitModifiersByMaskBoundary`. | A Morph group inside a Mirror renders mirrored rings; round-trips through export; mirror-inside-morph unchanged; regression test in `morph-modifier-panel.test.js`. | logged | `src/core/engine.js` (`_computeMorphGroups`, `getAncestorModifiers`), `tests/integration/morph-modifier-panel.test.js` |
| PRH-006 | Make Morph output canvas-click-selectable. | Morph children are `_morphConsumed` (return no interaction paths) and the morph group is a non-compound `isGroup`, which `findLayerAtPoint` skips — so clicking a visible ring selects nothing; the group is selectable only via the layers panel. Canvas selection/handles for modifier output were explicitly in the Morph "Deferred" bucket. | low | Mild UX friction — users must select the Morph group from the layers panel rather than by clicking its rings on canvas. | Teach `findLayerAtPoint` / selection-outline to hit-test a group carrying `morphedPaths` (treat like a compound), returning the group id. | Clicking a morph ring selects the morph group; selection outline encloses the morphed geometry; e2e/integration hit-test coverage. | **resolved (v1.1.76)** — dedicated `findMorphContainerAtPoint` hit-tests `morphedPaths` and selects the container as one object; double-click drills to a source child (`findMorphChildAtPoint`), Esc exits; bbox boxes the blend. Covered by `renderer-morph-group-edit.test.js` / `morph-child-transform.test.js` / `morph-down-routing.test.js`. | `src/render/renderer.js` (`findLayerAtPoint`), `src/core/engine.js` (`getRenderablePaths`) |
| PRH-007 | Prune the Petalis `petalisDesigner` config keys the wrapper force-overrides, and add bundler dead-key validation. | The `petalisdesigner.js` wrapper hard-overrides `ringMode`/`useDesignerShapeOnly`/`layoutMode` and zeroes `edgeWaveAmp`/`edgeWaveFreq`/`centerWaveBoost`/`centerSizeMorph`/`centerSizeCurve`/`centerShapeMorph`/`centerProfile`/`petalLengthRatio`/`petalSizeRatio`/`leafSidePos`/`leafSideWidth` on every generate, so the matching `ALGO_DEFAULTS.petalisDesigner` entries are dead — but removing keys risks breaking code that reads them, so it's a careful cleanup better done as final hardening than mid-overhaul. | low | `ALGO_DEFAULTS` keeps advertising a parameter surface the algorithm no longer honours, and authors/randomizers can keep writing dead keys into presets (silent preset rot). | Audit each wrapper-overridden key for readers, remove the truly-dead ones from `ALGO_DEFAULTS.petalisDesigner` (or move the enforcement into config), and add a `user-presets:bundle` warning when a preset carries a key the target layer type ignores. | Bundler warns on a preset authored with a dead key; `ALGO_DEFAULTS.petalisDesigner` advertises only honoured params; full suite green. | logged | `src/config/defaults.js`, `src/core/algorithms/petalisdesigner.js`, the user-presets bundler |
| PRH-008 | Thread `profilePoints` into the outline/rim/contour shading `buildPetal` calls so those types follow the designer/named profile. | The three internal `buildPetal` calls in `buildShadingLines` (outline/rim/contour) omit `profilePoints`, so they hatch the legacy silhouette and then get clipped against the real outline — fine for the common hatch types but wrong-shaped for outline-emphasis/rim/contour on a non-teardrop profile. Lower-impact than the other shading fixes already shipped, and the rim/contour copies need the profile scaled, so it was deferred. | low | Outline Emphasis / Rim Strokes / Contour Lines shading on a custom or non-teardrop profile draw a ghost of the wrong shape (mostly clipped away). | Pass `profilePoints` (scaled for the rim/contour inner copies) through the three `buildPetal` calls in `buildShadingLines`. | A visual test on a non-teardrop profile shows rim/contour strokes hugging the actual silhouette; full suite green. | logged | `src/core/algorithms/petalis.js` (`buildShadingLines`) |
| PRH-009 | Reconcile the Petal Designer profile editor with the rendered convex profile, and add a true geometric apical cleft. | (a) `buildProfileDesignerShape` synthesizes a 4-anchor drawn *approximation* of a named profile for the editor canvas; after the v1.1.83 convex-profile rewrite it no longer matches `buildLeafProfile`, so the designer's editor shows a slightly different shape than the canvas (and drawing converts to the approximation). (b) The heart/notched apical notch is a width-pinch near the tip, not a real bilobed cleft (the half-width model can't recess `x`). Both are advanced-path polish, deferred from the overhaul. | low | Designer power-users see the profile editor diverge from the canvas; obcordate/heart petals read as blunt rather than truly cleft. | (a) Seed the editor profile from the actual convex `petalShape` (or re-tune `buildProfileDesignerShape` to match), so opening + drawing is shape-consistent. (b) Add a tip post-process that splits the apex into two lobes with an inward cusp for `notch` profiles. | Opening the designer on a named-profile flower shows the same silhouette as the canvas; a heart/notched petal renders a real apical V; visual baselines added. | logged | `src/ui/ui-petal-designer.js` (`buildProfileDesignerShape`), `src/core/algorithms/petalis.js` (`petalShape`/`buildPetal`) |

| PRH-010 | Fix Petalis shading-line clipping for exactly axis-aligned petals — horizontal hatch/parallel lines on the 3 o'clock / 9 o'clock petals leak past the petal silhouette to the canvas edge. | Only reproduces in a degenerate configuration (petal at exactly 0°/180° + horizontal hatch lines, e.g. `parallel`/`radial` shading with `angle 0` on an even-count whorl) — the polygon clip's ray casting appears to degenerate when the test line is collinear with polygon vertices. Observed during the v1.1.92 preset sweep on a hatch-shading trial; no shipped preset hits it. | low | A user who adds parallel/hatch shading to a whorl flower sees the two horizontal petals spray hatch lines across the whole canvas (and into SVG/plotter output). | Harden `clipPathsInsidePolygon` (or the shading `makeLine` emission) against collinear/degenerate segments — e.g. nudge the ray-cast epsilon or pre-rotate the test frame for near-axis-aligned petals. | A whorl with petals at 0°/180° and a `parallel` shade at `angle 0` keeps every shading line inside the petal outline (unit test on segment containment); full suite green. | logged | `src/core/algorithms/petalis.js` (`clipPathsInsidePolygon`, `buildShadingLines`) |
| PRH-012 | Resolve Type-fill counter bleed on connected-script faces (e.g. Pacifico) whose glyph contours physically overlap. | The v1.2.14 watertight-fill rework makes every fill counter-tight for all NON-self-overlapping typefaces (verified across 8 faces incl. a high-contrast serif, a condensed, a display, and Lobster). Connected scripts where adjacent glyph *outers* overlap break the even-odd containment/depth classifier (`classifyRegionTopology`): a counter sitting under an overlapping neighbour outer is mis-depthed and can be filled. The principled fix is nonzero-winding ink for text, an engine-wide predicate change (all fill consumers + paint-bucket) carrying real regression risk to the common case — deferred. | low | On a heavily-connected script a per-shell fill (contour/voronoi/maze/…) may bleed into some letter counters; hatch/wave/dots are unaffected. Pre-fix these faces rendered far worse (contour drew only the first glyph), so this is a net improvement, not a regression. | Switch the text fill ink predicate to nonzero winding (relying on the font parser's outer/counter winding) so overlapping outers union while opposite-wound counters still carve — or special-case overlapping-outer groups in `classifyRegionTopology`. Gate to text only to protect paint-bucket/pattern even-odd semantics. | A Pacifico/connected-script word fills with 0 counter bleed and 0 empty shells across densities; non-script faces unchanged; full suite + watertight regression green. | **done (v1.2.15)** — text fills now use the nonzero winding rule, gated via a `windingRule` flag (`compositeContainsPoint`/`scanLineClipComposite` accumulate signed winding; `classifyRegionTopology` classifies shells by "inner band is ink" instead of even-odd depth). For non-overlapping glyphs nonzero ≡ even-odd, so non-script faces and all non-text consumers are byte-identical. Verified across 11 typefaces (5 connected scripts) × 15 fills × 4 words × 3 densities = 1,980 combos with 0 counter bleed; +10 regression tests (overlap union, counter carve, overlap-robust classification, even-odd-equivalence). | `src/core/algorithms/pattern.js` (`compositeContainsPoint`, `scanLineClipComposite`, `classifyRegionTopology`, `generatePatternFillPaths`), `src/core/algorithms/text.js`, `tests/unit/text-fill-watertight.test.js` |
| PRH-013 | Close the small Type-fill coverage gaps on `halftone` and `maze` at extreme density / oversized cell. | `halftone` at very high frequency (density ≈ 14) and `maze` whose cell size exceeds a thin shell's width can leave 1–2 small shells (a hairline serif feature) with no emitted geometry — a sampling/rasterization granularity limit, not a leak (counter bleed stays 0). Niche fills at non-default extremes; low beta probability. | low | A few tiny shells render unfilled under halftone/maze at extreme settings; every other fill covers them. | Sub-sample the halftone dot grid (guarantee ≥1 sample per shell) and clamp the maze cell size to a fraction of the shell inradius (or fall back to a centreline pass for sub-cell shells). | halftone @density 14 and maze with oversized cells fill every solid shell of a multi-glyph word (0 empty shells); full suite + watertight regression green. | logged | `src/core/algorithms/pattern.js` (`_halftoneFill`, `_mazeFill`) |
| PRH-015 | Idle/visibility bail-out for the HUD hint-bar `requestAnimationFrame` ticker so it stops spinning when nothing can change or the tab is hidden. | The Lane F HUD (`src/ui/shell/hint-bar.js`) polls tool/zoom/rotation on a continuous rAF loop (DOM writes are diffed, so it's cheap — a handful of property reads per frame — but the loop never yields). Adversarial review flagged it APPROVE-WITH-NIT: acceptable for beta, but a needless always-on frame callback. A `vectura:tool-changed` renderer event (interface request to Lane A) would let it become fully event-driven and retire the ticker. | low | Negligible steady-state CPU/battery cost from an always-scheduled rAF callback while the app is idle or backgrounded; no correctness or UX impact. | Pause the ticker on `document.hidden` (resume on `visibilitychange`) and/or stop scheduling after N consecutive no-change frames, re-arming on the pointer/tool/zoom events that can mutate the readouts. Prefer the Lane A `vectura:tool-changed` + wheel/zoom hooks so the loop can be dropped entirely. | With the tab hidden the rAF callback is not scheduled; readouts still update correctly on tool switch, zoom, and drag when visible; integration + e2e HUD suites stay green. | logged | `src/ui/shell/hint-bar.js` (`tick`/`start`); `tests/integration/hud-hint-bar.test.js` |
| PRH-011 | Add a `fastPreview` guard to the terrain river DEM hydrology so live-drag frames don't pay the full priority-flood + accumulation + carve over the full (up to 220×220) grid. | The v1.1.137 river rework runs the whole hydrology pipeline (priority-flood O(N log N), two sorts, accumulation, dendritic trace, per-cell carve) once per `generate()` with no preview shortcut, while sibling 3D algos shrink computation on `bounds.fastPreview`. Measured impact is modest at default settings (~9 ms added) but grows toward the 220×220 cap; an adversarial review flagged it (judged real-but-bounded, optional). | low | With Rivers enabled at high Depth Slices / X Resolution, dragging a terrain slider is heavier per frame than before (the old tracer was ~riverCount×≤800 steps). No correctness impact — output/determinism unchanged. | When `p.fastPreview`/`bounds.fastPreview` is set, compute the drainage network on a reduced grid (clamp the hydrology grid to `previewCap`, nearest-sample channel/carve back up) or memoize the network keyed by the heightfield-affecting params (it is independent of yaw/pitch/roll). | Dragging with rivers on at 300 slices / 600 resolution stays smooth; the committed (non-preview) render is unchanged and deterministic; full suite green. | logged | `src/core/algorithms/terrain.js` (river hydrology block) |
| PRH-014 | Split (or share) welded-kern contours in Outline Text so every non-whitespace glyph yields its own layer. | `TextOutlineOps.outlineText` (TXT-1) partitions the text layer's already-rendered geometry per glyph by nearest glyph-cell centroid. On parsed web faces with `mergeOverlaps` on, a kerned pair (RA/AV/LT…) or connected script welds two glyphs' ink into ONE contour whose centroid lands in a single glyph-quad — so the sibling glyph receives zero paths and produces no layer, diverging from the "glyph count = non-whitespace char count" acceptance. Geometry is fully preserved (no path dropped) and undo restores the editable Text layer, so it is a per-glyph-editability shortfall on connected scripts only, not a data-loss or render bug; the principled split needs polygon-clipping against each glyph-quad, deferred from Phase 1. | low | On a connected-script or tightly-kerned word, one glyph of a welded pair can't be selected/moved independently (its ink lives on the neighbour's layer). Ordinary text and non-overlapping faces are unaffected (glyph count is exact). | Option A: clip the welded ring against each contributing glyph-quad (`FillBoolean.intersection`) and assign the fragments to their respective glyph layers. Option B (Illustrator parity): keep welded glyphs as a single shared compound-path layer named for both characters. Pick per plotter semantics; gate to the merge-overlaps web-font path so the common case is untouched. | A welded AV/RA pair on a web face produces two glyph layers (Option A) or one clearly-labelled shared compound layer (Option B); glyph-count acceptance holds for the chosen semantics; the current-behavior documenting test in `tests/unit/text-outline-ops.test.js` is updated to the new contract; full suite green. A headless forced-weld regression (synthetic single-welded-contour input) already pins the CURRENT behavior; a full web-font weld repro needs async font parsing (not headlessly feasible today), so verify the fix in-browser on a real connected script too. | logged | `src/core/text-outline-ops.js` (`partitionPaths`/`outlineText`), `src/core/algorithms/text.js` (merge-overlaps weld), `tests/unit/text-outline-ops.test.js` |
| PRH-016 | Arrowhead stroke rows (start/end dropdown pair + swap + scale + align) in the Stroke Options panel. | Illustrator Tools Parity STR-6 explicitly defers these: they appeared in the source video's Stroke Options popover (Appendix A s20–46) but were never given values — only their enable/disable dependency on Align Stroke = center was exercised (f0025/f0044). Building them now would add plotter-meaningless dead UI and a non-trivial marker-geometry pipeline (arrowhead SVG `marker` defs + on-canvas synthesis + per-end scale/flip), out of scope for the v1 parity pass. Plotter semantics for arrowheads are themselves unclear (a pen plots the outline, not a filled marker), so the design needs its own study. | low | No functional gap today — Vectura has never had arrowheads; users drawing plotter line art don't expect them. If a future phase wants Illustrator-parity annotation strokes, the panel must grow the two dropdowns + swap/scale/align row. | Add an Arrowheads row beneath Align Stroke: a start/end marker `<select>` pair, a swap button, a Scale field, and a 2-way Align (extend/overhang). Persist `arrowStart`/`arrowEnd`/`arrowScale`/`arrowAlign` on the layer stroke model; synthesize marker geometry for canvas + emit SVG `marker` defs on export; disable while Align Stroke ≠ center (the video's observed dependency). | The panel shows arrowhead controls that round-trip through `.vectura` + SVG export, disable when Align Stroke ≠ center, and render a start/end marker on a plotted open path; full suite green. | logged | `src/ui/panels/stroke-options.js`, `src/config/stroke-options.js`, `src/core/layer.js`/`engine.js` (stroke model), `src/ui/ui-file-io.js` (SVG `marker` defs) |
| PRH-017 | Width Profile control (variable-width stroke profile dropdown) in the Stroke Options panel. | Illustrator Tools Parity STR-6 explicitly defers this: the video's Stroke Options popover showed a Profile dropdown ("Uniform" + width-profile presets) but never applied a non-uniform value (Appendix A s20–46). Vectura already has a per-path `meta.weightScale` variable-weight mechanism (silhouette/crease emphasis) exported as per-path `stroke-width` overrides, so a Profile control would be a UI over that — but wiring a whole-stroke width-profile ramp (thin→thick→thin along arc length) needs a profile-sampling pass and interacts with the STR-4 align offset and the banded-bold pen model, all out of scope for v1 parity. | low | No functional gap — uniform stroke width is the plotter norm and the existing `weightScale` covers emphasis. A future phase wanting Illustrator-style tapered strokes must add the profile ramp + its sampling. | Add a Profile `<select>` (Uniform + a small preset set) beneath Arrowheads; persist `widthProfile` on the stroke model; sample it along each path's normalized arc length into per-vertex `weightScale` (reusing the existing per-path override export at `ui-file-io.js`), reconciled with the STR-4 align offset. | The panel offers Uniform + tapered profiles that ramp a plotted stroke's width along its length and round-trip through export as per-vertex width overrides; Uniform is byte-identical to today; full suite green. | logged | `src/ui/panels/stroke-options.js`, `src/config/stroke-options.js`, `src/core/layer.js`/`engine.js` (stroke model), `src/ui/ui-file-io.js` (`weightScale` export path) |
| PRH-018 | Add most-recently-used (MRU) ordering to the Pen Picker popover's Pens tab (COL-1). | The Pen Picker popover (Illustrator Tools Parity Phase 1 Lane D) treats the document pen list as the video's "recent/available colors" surface; pens the user just created/applied naturally sit at the list tail, which satisfies the requirement. True MRU reordering (float the last-applied pen to the top) is called out by the SPEC (COL-1) as an explicit optional follow-up, not a blocker — reordering `SETTINGS.pens` would also reshuffle the docked Pens panel and plot-order grouping is penId-keyed (order-independent), so a *display-only* MRU is the safe form. Deferred to avoid a divergent ordering surface mid-lane. | low | Users with many pens must scan/scroll the full list to re-find a just-used pen rather than seeing it first; purely an ergonomic nicety. | Track an MRU timestamp/sequence per penId in a display-only map (not persisted into `SETTINGS.pens` order), and sort the popover's Pens-tab rows by it while leaving the docked panel and serialized pen order untouched. | Applying pen B then reopening the popover shows B first in the Pens tab; the docked Pens panel order and `.vectura` serialization are unchanged; full suite green. | logged | `src/ui/panels/pen-picker-popover.js` (`renderPenList`), `docs/illustrator-tools-parity-requirements.md` COL-1 |
| PRH-020 | Make the transform panel's W/H reflect a rotated layer's own object frame (not its axis-aligned preview bbox). | Illustrator Tools Parity SEL-5 (Phase 3 Lane K) shows true X/Y/W/H for manual shape/text layers, sourced from `renderer.getTransformPanelModel()` which measures the *axis-aligned* selection bounding box (`getSelectionBounds` corners). For an un-rotated layer (the common case) that equals the object's own W/H, and editing W resizes to the exact width. For a *rotated* single layer the readout is the rotated AABB, so W/H read larger than the geometry and `applySelectionBox` scales the AABB about its top-left rather than the object's local frame — a mismatch with Illustrator's "Transform panel shows object bounds, honours the reference-point rotation." Doing it correctly needs an unrotate→measure→rerotate pass (and a reference-point picker), its own surface, deferred from the parity pass. | low | Editing W/H (or reading it) on a rotated manual layer resizes/reports against the preview bbox, not the object's own width — surprising for rotated art. Un-rotated layers (the overwhelming majority) are exact; X/Y and Flip are unaffected. | Measure the selection box in the primary layer's rotated frame (unrotate corners by `params.rotation`, take min/max, re-rotate for display), and have `applySelectionBox` scale about the frame origin in that rotated basis; add a reference-point control so resize can pin a chosen corner/center. | A rotated square reports its true side length as W; setting W to a value resizes the geometry to exactly that width along its own axis; un-rotated behavior byte-identical; integration coverage on a rotated layer. | logged | `src/render/renderer.js` (`getTransformPanelModel`, `applySelectionBox`), `src/ui/panels/transform-panel.js`, `tests/integration/transform-numeric-xywh.test.js` |
| PRH-019 | Add the Simplify sub-mode's advanced-options gear (Illustrator "Simplify" dialog: separate curve-precision + angle-threshold sliders, straight-lines toggle, "Show Original Path" preview, point-count readout). | Illustrator Tools Parity TB-11 explicitly defers it: the source video shows an advanced-options gear on the Simplify sub-bar but never opens it (Appendix A f0065), so there is no observed behavior to build against. The current sub-mode ships the video-exercised controls — min/max wave icons, a single strength slider, Auto-Smooth, Done — plus a live `{pts} pts · {t} %` badge, which covers the demonstrated workflow. A full advanced panel would add a second simplify parameter surface (curve precision vs. angle threshold) that PathEditOps' single-`t` Visvalingam model does not currently expose, so it needs its own core design and is out of scope for the v1 parity pass. | low | No functional gap — the single strength slider + Auto-Smooth cover the plotter-relevant simplify need; power users cannot separately tune curve precision vs. corner-angle preservation, and there is no explicit numeric-threshold entry. | Add a gear button to the Simplify sub-bar opening an anchored panel with curve-precision and angle-threshold sliders, a "straighten near-straight segments" toggle, and a points-count readout; extend `PathEditOps.simplifyPreview` to accept a `{precision, angleThreshold, straighten}` options object (falling back to the current single-`t` behavior) so the panel and the strength slider drive the same session. | Opening the gear shows the advanced sliders; adjusting angle threshold preserves corners the strength slider alone would round; the strength slider and panel stay in one undo step; full suite green. | logged | `src/ui/shell/context-bar-modes.js` (Simplify sub-mode), `src/core/path-edit-ops.js` (`simplifyPreview`), `docs/illustrator-tools-parity-requirements.md` TB-11 |
| PRH-022 | Bound the Text-specimen renderer's `kick` re-render loop so a web face whose `ensureFont` resolves *without* populating the parsed store cannot spin the specimen forever. | Discovered while building TXT-3 hover live-preview (Phase 3 Lane J): `ui-text-specimen.js`'s `parsedFace()` re-`kick()`s `ensureFont(id)` on every render while `getParsed(id)` stays null, and the resolve handler calls `rerender()`. In production this always terminates — a real fetch either populates `WEBFONT_GLYPHS[id]` (loop stops) or rejects and sets `fontState='error'` (the reject branch clears `requested` and does NOT re-render). The infinite loop only manifests under a mocked/degenerate loader that resolves ensureFont successfully but leaves `getParsed` null (it hung the Lane J integration suite until the test mock was made to populate the parsed stub). So it is a latent robustness gap, not a shipped bug — hardening, not a fix. | low | No user-facing impact today: real loads terminate the loop. Risk is future-facing — a CDN that 200s an empty/unparseable TTF, or a parser that returns falsy, could make the specimen re-render in a tight promise loop (pinned CPU) rather than settling to the fallback face. | In `kick`, treat a resolve that did not change `getParsed(id)` as terminal for that id (e.g. cap consecutive no-progress re-kicks per key, or only `rerender()` when the parsed face actually became available), so a successful-but-empty load falls back to the UI-font specimen once instead of looping. | A loader stubbed to resolve `ensureFont` without populating the store renders the specimen once and stops (no repeated `rerender`); real loads and the error path are unchanged; specimen unit/integration suites green. | logged | `src/ui/ui-text-specimen.js` (`kick`, `parsedFace`), `tests/integration/text-font-hover-preview.test.js` |
| PRH-021 | Add a real clipboard subsystem (Cut / Copy / Paste / Paste-in-Place) for layers, and surface it in the canvas context menu (CTX-1). | Illustrator Tools Parity CTX-1 explicitly scopes a clipboard OUT: Vectura has no Cut/Copy/Paste model, so the context menu ships `Duplicate` (the existing ⌘D `duplicateLayers`) as the equivalent verb. A true clipboard is a cross-cutting new subsystem — an in-memory (and ideally system-clipboard-backed) serialize/deserialize of layer sets with id remapping, paste-offset/paste-in-place placement, cross-document paste, and undo integration — with real design surface (pen/penId reconciliation, group/compound/modifier container handling, mask references) that was deliberately not built for the v1 parity pass. | low | No functional gap for single-document work — Duplicate covers "copy this here"; users cannot copy a selection to a different position/document via the clipboard, and there is no ⌘C/⌘V. | Add `Vectura.ClipboardOps` (copy/cut serialize selected layers to an internal buffer + `navigator.clipboard` where available; paste deserializes with fresh ids, a configurable paste offset, and a paste-in-place variant), wire ⌘X/⌘C/⌘V/⇧⌘V in `src/ui/shortcuts.js`, and add Cut/Copy/Paste/Paste-in-Place rows to the CTX-1 menu (enabled by buffer + selection state). | Copy then paste reproduces the selection at an offset with new ids under one undo step; paste-in-place lands at the original coordinates; cut removes the source; the context menu rows enable/disable correctly; full suite green. | logged | `src/ui/shell/canvas-context-menu.js` (CTX-1), `src/ui/shortcuts.js`, new `src/core/clipboard-ops.js`, `docs/illustrator-tools-parity-requirements.md` CTX-1 |

## Operating Model

1. During beta, log applicable ideas as `logged` and continue development unless explicitly requested otherwise.
2. Before final public release, review all `PRH-*` items, mark chosen items as `selected`, implement them, then mark them `done`.
3. After release, carry unfinished items forward or mark them `dropped` with rationale.
